# Process Explainers

Interactive, self-contained HTML explanatory models of complex processes.
Built with the `matt-process-visualizer` skill (SVG + WAAPI) and friends.

**1 explainer** · index auto-generated by `scripts/build-index.mjs` — edit `meta.json` files, not this table.

## web

| Explainer | Summary | Format | Date |
|---|---|---|---|
| [FastAPI Request Flow: Client to Response](explainers/fastapi-request-flow/index.html) | How an HTTP request travels through uvicorn, FastAPI routing, dependency injection, Pydantic validation, the path-operation function, and back out as a serialized JSON response. | svg-waapi | 2026-08-08 |

## Adding an explainer

1. Copy `explainers/_template/` to `explainers/<slug>/`
2. Drop the self-contained `index.html` in it and fill in `meta.json`
3. Run `node scripts/build-index.mjs`
